const {SlashCommandBuilder}=require('discord.js');const {warningEmbed}=require('../embeds');const {db,now}=require('../db');const {requireStaff}=require('../utils');
module.exports={data:new SlashCommandBuilder().setName('warn').setDescription('Gives a member an official warning.').addUserOption(o=>o.setName('member').setDescription('Member').setRequired(true)).addStringOption(o=>o.setName('reason').setDescription('Reason').setRequired(true)),async execute(i){if(!(await requireStaff(i)))return;const m=i.options.getUser('member');const reason=i.options.getString('reason');const r=db.prepare('INSERT INTO warnings(guild_id,user_id,moderator_id,reason,created_at) VALUES(?,?,?,?,?)').run(String(i.guild.id),String(m.id),String(i.user.id),reason,now());return i.reply({embeds:[warningEmbed('WARNING ISSUED',`${m} has been warned.`,[['Warning ID',`#${r.lastInsertRowid}`,true],['Reason',reason,false],['Moderator',i.member.toString(),true]])]})}};
