const {SlashCommandBuilder}=require('discord.js');const {errorEmbed,warningEmbed,successEmbed,infoEmbed}=require('../embeds');const {db,now}=require('../db');const {requireStaff}=require('../utils');
const data=new SlashCommandBuilder().setName('report').setDescription('Reports a member to moderators for review.').addUserOption(o=>o.setName('member').setDescription('Member').setRequired(true)).addStringOption(o=>o.setName('reason').setDescription('Reason').setRequired(true)).addStringOption(o=>o.setName('evidence').setDescription('Evidence URL'));
module.exports={data,async execute(i){const m=i.options.getUser('member');const reason=i.options.getString('reason');const evidence=i.options.getString('evidence');const r=db.prepare('INSERT INTO reports(guild_id,reporter_id,reported_id,reason,evidence_url,created_at) VALUES(?,?,?,?,?,?)').run(String(i.guild.id),String(i.user.id),String(m.id),reason,evidence||null,now());return i.reply({embeds:[warningEmbed('REPORT SUBMITTED','Your report has been submitted for moderator review.',[['Report ID',`#${r.lastInsertRowid}`,true],['Member',m.toString(),true],['Reason',reason,false]])],ephemeral:true})}};
