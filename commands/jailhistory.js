const {SlashCommandBuilder}=require('discord.js');const {infoEmbed}=require('../embeds');const {db}=require('../db');const {requireStaff}=require('../utils');
module.exports={data:new SlashCommandBuilder().setName('jailhistory').setDescription('Shows a member jail history.').addUserOption(o=>o.setName('member').setDescription('Member').setRequired(true)),async execute(i){if(!(await requireStaff(i)))return;const m=i.options.getUser('member');const rows=db.prepare('SELECT * FROM jail_cases WHERE guild_id=? AND user_id=? ORDER BY created_at DESC LIMIT 25').all(String(i.guild.id),String(m.id));return i.reply({embeds:[infoEmbed(`JAIL HISTORY  /  ${m.username}`,rows.length?rows.map(c=>`**CASE #${c.case_id}**  ${c.status.toUpperCase()}  •  <t:${c.created_at}:f>\n${c.reason||'No reason provided'}`).join('\n\n'):`${m} has no jail history.`)]})}};
